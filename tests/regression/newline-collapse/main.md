# Newline Collapse

This paragraph is soft-wrapped across
several source lines that Confluence would
otherwise render as hard breaks, so they
collapse to spaces.

```
code lines
keep their
newlines
```

A final soft-wrapped
paragraph.
