# Link Not Yet Published

A link to a sibling that exists on disk but has no `page_id` yet is a
Warning, not Broken -- the normal state of every page in a tree that hasn't
been published, not a defect. The href renders exactly as written, unlike a
genuinely missing or escaping target:

[the draft](draft.md)
