# Code Blocks

A fenced block with a language:

```python
def greet(name):
    return f"hello, {name}"
```

A fenced block with no language:

```
plain text
  indented line
```

A block containing a CDATA terminator that must be escaped:

```xml
<note><![CDATA[danger ]]> here]]></note>
```
